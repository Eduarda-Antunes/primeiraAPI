const express = require('express'); //esse pacote quer utilizar o server

const app = express(); //cria uma instância do express

app.listen(3000, () => console.log('Running at port 3000')) //qual porta o servidor vai ouvir + eurofunction para testar

